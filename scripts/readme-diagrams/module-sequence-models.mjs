import { branch, msg } from "./lib/sequence-renderer.mjs";

const coreSource = "leader-core/README.md; leader-core/README.ko.md; leader-core/src/main/kotlin/io/bluetape4k/leader/LeaderElector.kt; leader-core/src/main/kotlin/io/bluetape4k/leader/coroutines/SuspendLeaderElector.kt";
const coreEvidence = "leader-core README sequence sections, LeaderElector contracts, SuspendLeaderElector contracts";

function singleBackendSequence({ slug, title, subtitle, moduleName, backendLabel, acquire, release, recovery, sourceRead, evidence }) {
  return {
    slug,
    title,
    subtitle,
    desc: `${title} sequence diagram.`,
    intent: `Answer the ${moduleName} reader's single-leader runtime question: which caller message attempts ownership, which backend operation decides success or skip, how the protected body runs, and how release or lease recovery prevents stale ownership.`,
    evidence,
    sourceRead,
    participants: [
      { id: "caller", label: "Caller", color: "blue" },
      { id: "elector", label: `${moduleName} elector`, color: "green" },
      { id: "backend", label: backendLabel, color: "amber" },
      { id: "body", label: "Leader body", color: "teal" },
      { id: "cleanup", label: "Recovery path", color: "purple" },
    ],
    events: [
      msg(0, 1, "runIfLeader(lockName, options, body)", "call"),
      msg(1, 2, acquire, "contention"),
      branch("alt ownership acquired"),
      msg(2, 1, "token accepted", "success", true),
      msg(1, 3, "execute protected leader body", "call"),
      msg(3, 1, "body result", "success", true),
      msg(1, 2, release, "release"),
      msg(1, 0, "return body result", "success", true),
      branch("else contention or crash"),
      msg(2, 1, "existing owner token blocks acquire", "skip", true),
      msg(1, 4, recovery, "dependency"),
      msg(1, 0, "return skipped result", "skip", true),
    ],
  };
}

function groupBackendSequence({ slug, title, subtitle, moduleName, backendLabel, slotAcquire, release, sourceRead, evidence }) {
  return {
    slug,
    title,
    subtitle,
    desc: `${title} sequence diagram.`,
    intent: `Answer the ${moduleName} reader's group-election question: how bounded slot ownership differs from a single lock, where maxLeaders is enforced, and which backend operation releases the chosen slot after work completes.`,
    evidence,
    sourceRead,
    participants: [
      { id: "caller", label: "Caller", color: "blue" },
      { id: "group", label: `${moduleName} group elector`, color: "teal" },
      { id: "slots", label: "Slot namespace", color: "amber" },
      { id: "backend", label: backendLabel, color: "purple" },
      { id: "body", label: "Group body", color: "green" },
    ],
    events: [
      msg(0, 1, "runIfLeader(groupName, maxLeaders, body)", "call"),
      msg(1, 2, "build slot keys 0..maxLeaders-1", "dependency"),
      msg(1, 3, slotAcquire, "contention"),
      branch("alt slot acquired"),
      msg(3, 1, "slot token accepted", "success", true),
      msg(1, 4, "execute body for owned slot", "call"),
      msg(4, 1, "slot result", "success", true),
      msg(1, 3, release, "release"),
      msg(1, 0, "return group result and slot", "success", true),
      branch("else all slots busy"),
      msg(3, 1, "no slot accepted before waitTime", "skip", true),
      msg(1, 0, "return skipped group result", "skip", true),
    ],
  };
}

export const moduleSequences = [
  singleBackendSequence({
    slug: "leader-core-sequence-02",
    title: "Core Single-Leader Acquire Release Sequence",
    subtitle: "Local and backend electors share the same acquire, execute, release, or skip contract.",
    moduleName: "Core",
    backendLabel: "Lock primitive",
    acquire: "tryAcquire(lockName, waitTime, leaseTime)",
    release: "release owned token after minLeaseTime",
    recovery: "lease expiry or local state clears stale owner",
    evidence: coreEvidence,
    sourceRead: coreSource,
  }),
  groupBackendSequence({
    slug: "leader-core-sequence-03",
    title: "Core Group Slot Acquire Release Sequence",
    subtitle: "A bounded slot namespace turns maxLeaders into semaphore-style leader election.",
    moduleName: "Core",
    backendLabel: "Slot primitive",
    slotAcquire: "tryAcquire first free slot token",
    release: "release owned slot token",
    evidence: coreEvidence,
    sourceRead: coreSource,
  }),
  singleBackendSequence({
    slug: "leader-hazelcast-sequence-02",
    title: "Hazelcast Lock Acquire Release Sequence",
    subtitle: "IMap.putIfAbsent owns the lock key and conditional remove releases only the owner token.",
    moduleName: "Hazelcast",
    backendLabel: "Hazelcast IMap",
    acquire: "IMap.putIfAbsent(lockKey, token, leaseTime)",
    release: "IMap.remove(lockKey, token)",
    recovery: "Hazelcast TTL removes crashed owner key",
    evidence: "leader-hazelcast README lock map notes and Hazelcast elector source",
    sourceRead: "leader-hazelcast/README.md; leader-hazelcast/README.ko.md; leader-hazelcast/src/main/kotlin/io/bluetape4k/leader/hazelcast/HazelcastLeaderElector.kt",
  }),
  groupBackendSequence({
    slug: "leader-hazelcast-sequence-03",
    title: "Hazelcast Group Slot Sequence",
    subtitle: "N IMap slot keys act as a distributed semaphore for concurrent leaders.",
    moduleName: "Hazelcast",
    backendLabel: "Hazelcast IMap",
    slotAcquire: "putIfAbsent(slotKey, token, leaseTime)",
    release: "remove(slotKey, token)",
    evidence: "leader-hazelcast README group slot notes and Hazelcast group elector source",
    sourceRead: "leader-hazelcast/README.md; leader-hazelcast/README.ko.md; leader-hazelcast/src/main/kotlin/io/bluetape4k/leader/hazelcast/HazelcastLeaderGroupElector.kt",
  }),
  singleBackendSequence({
    slug: "leader-k8s-sequence-02",
    title: "Kubernetes Lease Acquire Release Sequence",
    subtitle: "Lease holder identity and renewTime decide ownership, skip, and recovery behavior.",
    moduleName: "Kubernetes",
    backendLabel: "Kubernetes Lease",
    acquire: "create or replace Lease with holderIdentity",
    release: "clear holder identity when token still matches",
    recovery: "leaseDurationSeconds expires stale holder",
    evidence: "leader-k8s README and Kubernetes lease elector source",
    sourceRead: "leader-k8s/README.md; leader-k8s/README.ko.md; leader-k8s/src/main/kotlin/io/bluetape4k/leader/k8s/K8sLeaderElector.kt",
  }),
  singleBackendSequence({
    slug: "leader-redis-lettuce-sequence-02",
    title: "Lettuce Redis Acquire Release Recovery Sequence",
    subtitle: "SET NX PX owns the lock, Lua compares the token, and TTL recovers crashed owners.",
    moduleName: "Lettuce Redis",
    backendLabel: "Redis key",
    acquire: "SET lockKey token NX PX leaseTime",
    release: "Lua compare token then DEL",
    recovery: "Redis TTL deletes unreleased crash token",
    evidence: "leader-redis-lettuce README scenario 1 and Lettuce elector source",
    sourceRead: "leader-redis-lettuce/README.md; leader-redis-lettuce/README.ko.md; leader-redis-lettuce/src/main/kotlin/io/bluetape4k/leader/redis/lettuce/LettuceLeaderElector.kt",
  }),
  singleBackendSequence({
    slug: "leader-redis-lettuce-sequence-03",
    title: "Lettuce Redis Minimum Lease Sequence",
    subtitle: "Release keeps the backend TTL alive until minLeaseTime has elapsed.",
    moduleName: "Lettuce Redis",
    backendLabel: "Redis TTL",
    acquire: "SET lockKey token NX PX leaseTime",
    release: "update PTTL or DEL after minLeaseTime",
    recovery: "backend TTL enforces remaining minimum lease",
    evidence: "leader-redis-lettuce README scenario 2 and Lettuce minLease source",
    sourceRead: "leader-redis-lettuce/README.md; leader-redis-lettuce/README.ko.md; leader-redis-lettuce/src/main/kotlin/io/bluetape4k/leader/redis/lettuce/LettuceLeaderElector.kt",
  }),
  singleBackendSequence({
    slug: "leader-redis-redisson-sequence-02",
    title: "Redisson Acquire Release Recovery Sequence",
    subtitle: "RLock ownership, watchdog renewal, and unlock checks protect the leader body.",
    moduleName: "Redisson",
    backendLabel: "Redisson RLock",
    acquire: "tryLock(waitTime, leaseTime)",
    release: "unlock only when current owner still holds lock",
    recovery: "watchdog or lease expiry clears abandoned lock",
    evidence: "leader-redis-redisson README scenario 1 and Redisson elector source",
    sourceRead: "leader-redis-redisson/README.md; leader-redis-redisson/README.ko.md; leader-redis-redisson/src/main/kotlin/io/bluetape4k/leader/redis/redisson/RedissonLeaderElector.kt",
  }),
  singleBackendSequence({
    slug: "leader-redis-redisson-sequence-03",
    title: "Redisson Minimum Lease Sequence",
    subtitle: "updateLeaseTime preserves lock-at-least-until semantics before final unlock.",
    moduleName: "Redisson",
    backendLabel: "Redisson RLock",
    acquire: "tryLock(waitTime, leaseTime)",
    release: "updateLeaseTime or unlock after minimum hold",
    recovery: "remaining lease protects minimum hold window",
    evidence: "leader-redis-redisson README scenario 2 and Redisson lease source",
    sourceRead: "leader-redis-redisson/README.md; leader-redis-redisson/README.ko.md; leader-redis-redisson/src/main/kotlin/io/bluetape4k/leader/redis/redisson/RedissonLeaderElector.kt",
  }),
  {
    slug: "leader-spring-boot-sequence-02",
    title: "Spring Boot Reentrant Leader Election Sequence",
    subtitle: "AOP opens a leader scope, nested calls reuse the scope, and LockAssert sees only real ownership.",
    desc: "Spring Boot reentrant leader election sequence diagram.",
    intent: "Answer the Spring Boot reader's AOP runtime question: how does an annotated method enter leader election, why do nested leader calls avoid reacquiring the same backend lock, and how does the scope close after the protected work returns?",
    evidence: "leader-spring-boot README reentrant section, LeaderElectionAspect, LockAssert, LockExtender",
    sourceRead: "leader-spring-boot/README.md; leader-spring-boot/README.ko.md; leader-spring-boot/src/main/kotlin/io/bluetape4k/leader/spring/aop/LeaderElectionAspect.kt; leader-spring-boot/src/main/kotlin/io/bluetape4k/leader/spring/LockAssert.kt",
    participants: [
      { id: "caller", label: "Caller", color: "blue" },
      { id: "aspect", label: "LeaderElectionAspect", color: "green" },
      { id: "scope", label: "Leader scope", color: "purple" },
      { id: "elector", label: "Backend elector", color: "amber" },
      { id: "method", label: "Annotated method", color: "teal" },
      { id: "assert", label: "LockAssert", color: "olive" },
    ],
    events: [
      msg(0, 1, "invoke @LeaderElection method", "call"),
      msg(1, 2, "check active leader scope", "dependency"),
      branch("alt outer call"),
      msg(1, 3, "runIfLeader via configured elector", "contention"),
      msg(3, 1, "ownership acquired", "success", true),
      msg(1, 2, "open Real leader scope", "success"),
      msg(1, 4, "proceed with annotated body", "call"),
      msg(4, 5, "assertLocked inside scope", "metric"),
      msg(4, 1, "return method result", "success", true),
      msg(1, 2, "close scope and release lock", "release"),
      branch("else nested call"),
      msg(2, 1, "reuse existing Real scope", "success", true),
      msg(1, 4, "proceed without backend reacquire", "call"),
      msg(1, 0, "return method result", "success", true),
    ],
  },
];
