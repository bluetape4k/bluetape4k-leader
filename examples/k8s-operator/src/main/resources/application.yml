demo:
  operator:
    namespace: ${POD_NAMESPACE:default}
    pod-name: ${HOSTNAME:local-operator}
    node-id: ${HOSTNAME:local-operator}
    lock-name: cronjob-reconciler
    wait-time: PT1S
    lease-time: PT30S
    initial-delay-ms: 1000
    fixed-delay-ms: 5000

management:
  endpoints:
    web:
      exposure:
        include: health,info
