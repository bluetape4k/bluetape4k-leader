global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/rules/leader-alerts.yml

scrape_configs:
  - job_name: bluetape4k-leader
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - app:8080
