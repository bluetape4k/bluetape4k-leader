# Leader 0.4 Manual Diagram Review

Reviewed on 2026-07-15 against the `bluetape-diagram` common and architecture
contracts. The release model is pinned by `docs/manual/manifest.yaml` to
`0.4.0` at `17ab7f872c1f96318c73d3580729cac20a67e017`.

## Scope and source model

The five canonical SVG/PNG pairs answer these reader questions:

| Asset | Reader question | Manual and source anchors |
| --- | --- | --- |
| `docs/manual/assets/overview/repository-learning-map` | In what order should a reader learn the repository? | `docs/manual/en/index.md`, `docs/manual/en/architecture/repository-map.md`, `AGENTS.md`, release inventory |
| `docs/manual/assets/architecture/election-lifecycle` | Where do contention, action failure, cancellation, retry, extension, and release sit? | `docs/manual/en/architecture/runtime-model.md`, `leader-core/.../LeaderElector.kt`, `LeaderRunResult.kt` |
| `docs/manual/assets/architecture/model-decision-map` | Which election and execution model should be selected before a backend? | `docs/manual/en/guides/election-model-selection.md`, blocking/async/virtual-thread/suspend/strategic interfaces |
| `docs/manual/assets/backends/backend-selection-map` | Which already-operated coordination system best matches the required semantics? | `docs/manual/en/guides/backend-selection.md`, release module inventory |
| `docs/manual/assets/frameworks/framework-observability-flow` | How do Spring Boot and Ktor preserve the same ownership and observability boundaries? | `docs/manual/en/guides/spring-vs-ktor.md`, `LeaderElectionAutoConfiguration.kt`, `LeaderElectionPlugin.kt`, `MicrometerLeaderElectionListener.kt` |

The approved visual family is the existing Leader manual dark canvas generated
by `scripts/manual/render_leader_diagrams.rb`: dark navy canvas and cards,
Architects Daughter/monospace text, restrained cyan/teal/purple/amber/rose
semantics, and explicit matching arrowheads. No infrastructure icons apply;
all nodes are library concepts or decision categories.

## Corrected invariants

- Every multi-turn connector uses orthogonal segments with rounded `Q` bends.
- Fan-out routes use distinct ports and corridors; colored trunks do not overlap.
- The election retry route runs above the lifecycle while cancellation runs below
  `Run`; they no longer cross.
- Every card is marked with `class="card"`, so connector intrusion checks count
  real card geometry instead of reporting the earlier weak `cards=0` result.
- Each semantic color has an explicit `14x14`, `userSpaceOnUse` marker.
- PNG generation is canonical CairoSVG CLI output at scale 2
  (`cairosvg <svg> -o <png> -s 2`).

## One-asset review ledger

Each row was completed in order: generate one SVG/PNG pair, parse XML, run all
connector gates, inspect the 3200x2080 PNG at original size, then continue.

| Asset | Connectors / cards | Intrusions / crossings | Geometry | Endpoint | Paths / Q bends / mixed failures | Full-size PNG inspection |
| --- | ---: | ---: | ---: | --- | ---: | --- |
| `architecture/election-lifecycle` | 7 / 9 | 0 / 0 | 0 failures | PASS | 7 / 4 / 0 | PASS: retry and cancellation are separated; dashed lines retain solid, visible heads; all card text fits |
| `architecture/model-decision-map` | 7 / 7 | 0 / 0 | 0 failures | PASS | 7 / 10 / 0 | PASS: three model branches and two execution targets have distinct readable corridors |
| `backends/backend-selection-map` | 7 / 9 | 0 / 0 | 0 failures | PASS | 7 / 8 / 0 | PASS after repair: an initial cluster/control-plane crossing was detected and removed; five colors remain distinguishable |
| `frameworks/framework-observability-flow` | 8 / 7 | 0 / 0 | 0 failures | PASS | 8 / 12 / 0 | PASS: Spring/Ktor merge ports, backend/event fan-out, and backend -> release -> events direction are clear |
| `overview/repository-learning-map` | 7 / 6 | 0 / 0 | 0 failures | PASS | 7 / 8 / 0 | PASS: three learning branches and workshop convergence remain separated; no label or arrowhead is clipped |

All five PNGs are exactly `3200x2080`. Full-size review checked title and footer
bounds, every card label, every port, rounded corner continuity, arrow direction,
arrowhead size/color, unrelated-line crossings, card intrusion, and canvas
whitespace. No text touches or exceeds a card boundary.

## Commands and exposure

```bash
ruby scripts/manual/render_leader_diagrams.rb
xmllint --noout docs/manual/assets/**/*.svg
python3 ~/.codex/skills/bluetape-diagram/scripts/diagram-connector-audit.py <svg>
python3 ~/.codex/skills/bluetape-diagram/scripts/diagram-geometry-audit.py --fail-diagonal <svg>
python3 ~/.codex/skills/bluetape-diagram/scripts/diagram-endpoint-audit.py <svg>
python3 ~/.codex/skills/bluetape-diagram/scripts/diagram-mixed-corner-audit.py <svg>
ruby scripts/manual/validate_diagrams.rb
git diff --check -- scripts/manual/render_leader_diagrams.rb scripts/manual/validate_diagrams.rb docs/manual/assets docs/superpowers/notes/2026-07-14-leader-0.4-diagram-review.md
```

The final aggregate run reported five XML-valid SVG/PNG pairs, 36 directed
connectors, 38 auditable cards, 42 rounded `Q` bends, zero intrusions, zero
crossings, zero geometry failures, zero endpoint failures, and zero mixed-corner
failures. English and Korean manuals expose every asset; the repository map is
also exposed from both language index pages.

Required checks: 17/17; N/A: 1 (infrastructure icon gate, because no node is an
external infrastructure component); Blocked: 0.
