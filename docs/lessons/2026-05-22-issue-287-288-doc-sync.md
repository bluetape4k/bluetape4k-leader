# 문제 287/288 README 및 KDoc 동기화

## 맥락

Issue #287 및 #288은 `bluetape4k-leader`에 남아 있는 작은 문서 공백이었습니다. 루트 한국어 README 콘텐츠는 관리/관찰 가능성 문서에 뒤처졌고 `LeaderElectionManagementRegistry`는 공개 KDoc 행동/계약 표준을 충족하지 않았습니다.

## 결정

Spring Boot Actuator 엔드포인트와 Ktor 관리 경로 섹션을 두 로케일 모두에 추가하여 루트 `README.md`와 `README.ko.md`를 동기화된 상태로 유지하세요. `LeaderElectionManagementRegistry`용 공개 KDoc만 확장합니다. 행동을 바꾸지 마십시오.

## 결과

이제 루트 README에 `GET /actuator/leaderElection`, `bluetape4k.leader.observability.lock-names` 및 `GET /management/leaderElection`가 문서화되어 있습니다. Ktor 레지스트리 KDoc는 이제 스레드 안전성, 결정적 스냅샷, JVM 로컬 범위 및 기본 사용법을 명시합니다.

## 검증

- `git diff --check`
- `./gradlew :bluetape4k-leader-ktor:compileKotlin`
- `./gradlew :bluetape4k-leader-ktor:test` (17통과)

## 퓨쳐 가드

모듈 README 콘텐츠가 루트 README로 승격되면 동일한 변경 사항으로 기존 지역화된 루트 README를 업데이트하고 엔드포인트 ID, 속성 및 경로 경로에 대한 실제 소스 이름을 grep-check하십시오.
