# 카탈로그 참조 2026-05-25-01

## 맥락

공유 `bluetape4k-dependencies` 카탈로그는 ASM, AWS Kotlin SDK 및 Apache Fory에 대한 종속성 업데이트를 받았습니다.

## 결정

`develop`에 플로팅하는 대신 기본 `bluetape4kDependenciesCatalogRef`를 `catalog/2026-05-25-01`에 고정합니다.

## 결과

Gradle 설정은 이제 `bluetape4kDependenciesCatalogRef` 또는 `BLUETAPE4K_DEPENDENCIES_CATALOG_REF`를 통한 재정의를 허용하면서 기본적으로 릴리스 트레인 카탈로그를 가져옵니다.

스냅샷 게시 워크플로는 이제 `develop`를 강제하는 대신 전달된 참조를 검증하므로 분기 게이트 스냅샷 열차 유효성 검사는 Nightly를 통과한 동일한 카탈로그 고정 소스를 게시합니다.

## 검증

- 설정 파일이 `catalog/2026-05-25-01`로 업데이트되었습니다.
- `./gradlew compileTestKotlin --refresh-dependencies --console=plain -PsnapshotVersion=-SNAPSHOT`
- `actionlint .github/workflows/publish-snapshot.yml`

## 퓨쳐 가드

조정된 종속성 트레인 업데이트를 위해 날짜가 지정된 카탈로그 태그를 사용하세요. 수동으로 디스패치된 스냅샷 게시 워크플로우에서 `develop` 체크아웃을 강제하지 마십시오. 유효성 검사와 게시가 동일한 소스 트리를 사용하도록 디스패치 참조를 사용하세요.
